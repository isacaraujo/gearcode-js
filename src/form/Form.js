;(function ($, gc) {

    gc.form = gc.form || {};

    gc.form.Form = gc.display.DisplayObject.extend({

        init: function (options) {
            this._super(options);
        }
    });

}(jQuery, window.gc = window.gc || {}));
